#include<bits/stdc++.h>
#include<stdio.h>
#include<string.h>
#include<math.h>
using namespace std;

	//conditional oparator
	//(exp1)? true :false;
	
	int main()
		{
			int a,b,sum,c;
			scanf("%d %d",&a,&b);
			//(a+b)^2=a^2+2ab+b^2
			c=a+b;
			sum=c*c;
			printf("the result is: %d",sum);
			
			//sum=(a,b,a+b);
			//printf("the sum is: %d\n",sum);
		
			//(a>b) ? printf("Result is A: %d",a) :printf("Result Is B:%d",b);
			
			return 0;
		}
